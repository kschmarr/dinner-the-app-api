CREATE TABLE dinner_users (
    userid INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    username TEXT NOT NULL,
    token TEXT NOT NULL,
    meal_index INTEGER DEFAULT 1 NOT NULL,
    short_index INTEGER DEFAULT 1 NOT NULL,
    medium_index INTEGER DEFAULT 1 NOT NULL,
    long_index INTEGER DEFAULT 1 NOT NULL
);